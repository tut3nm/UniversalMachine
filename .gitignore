# Cada subcarpeta (máquina232/, Diagramadora/) tiene su propio .gitignore
# más específico. Esto es solo para basura de nivel raíz / herramientas.

# OS / editor
.DS_Store
Thumbs.db
*.swp

# front/ + backend/ (sistema web nuevo, ver PLAN_WEBAPP.md)
node_modules/
.venv/
venv/
__pycache__/

# build.bat: salida de PyInstaller (front/dist/ ya lo cubre front/.gitignore)
/dist/
/build/
